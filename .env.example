# Variables para la base de datos externa de Supabase (no Lovable Cloud).
# Copia estos nombres en Cloudflare Pages / Workers → Settings → Environment variables.
# NUNCA uses la service_role key aquí; solo la anon/public key.

NODE_VERSION=20
VITE_SUPABASE_EXTERNAL_URL=https://ujxxoagfdrkgbssutjnn.supabase.co
VITE_SUPABASE_EXTERNAL_ANON_KEY=tu_anon_key_de_supabase
