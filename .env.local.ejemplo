# Conexión al Supabase propio de Family Help.
# Copia este archivo como `.env.local` (no se sube a Git) y completa la clave.
# En Cloudflare Pages, define las mismas dos variables en Settings > Environment variables.

VITE_SUPABASE_EXTERNAL_URL=https://ujxxoagfdrkgbssutjnn.supabase.co
VITE_SUPABASE_EXTERNAL_ANON_KEY=pega-aqui-la-clave-anon-publishable
